{% extends "admin/base.html.twig" %}

{% block title %}Infos. Générales{% endblock %}

{% block body %}
    <div class="box">
        <div class="box-body">
            {{ form_start(form) }}

            {{ form_row(form.home_title) }}
            {{ form_row(form.home_text) }}

            <input type="submit" class="btn btn-primary btn-block" value="Envoyer" />
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
